import { useLocation } from "react-router-dom";
const FullCast = () => {
    const { castData } = useLocation()
    return (
        <div>
            FullCast
        </div>
    )
};

export default FullCast;